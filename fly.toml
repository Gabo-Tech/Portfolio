# fly.toml app configuration file generated for portfolio-kc3z1a on 2025-08-25T20:14:05Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'portfolio-kc3z1a'
primary_region = 'cdg'

# ────────────────────────────────
# BUILD CONFIGURATION
# ────────────────────────────────
[build]
  dockerfile = "Dockerfile"   # (optional – Fly will use Dockerfile by default)

  # <-- IMPORTANT: Pass EmailJS keys at **build time**
  [build.args]
    NEXT_PUBLIC_SERVICE_ID = "service_j65320s"
    NEXT_PUBLIC_TEMPLATE_ID = "template_yirel6n"
    NEXT_PUBLIC_PUBLIC_KEY = "bALVoDGnok_LWsWZN"

# ────────────────────────────────
# RUNTIME SERVICE
# ────────────────────────────────
[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

# ────────────────────────────────
# VM SETTINGS
# ────────────────────────────────
[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
